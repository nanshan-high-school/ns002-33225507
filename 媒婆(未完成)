#include <iostream>
#include <stack>
using namespace std; 
int main() {
    stack <int> people;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        char p;
        cin >> p;
        people.push(p);
    }
}
